// <==================== file for the lazy loading ============>

// importing the required modules

const LazyLoadingComp = () => {
  return (
    <div>
      <p>lazy loaded component</p>
    </div>
  );
};

export default LazyLoadingComp;
